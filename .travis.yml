sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sergm1/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true sergm1/react-test npm test

after_success:
  - docker build -t sergm1/multi-client ./client
  - docker build -t sergm1/multi-nginx ./nginx
  - docker build -t sergm1/multi-server ./server
  - docker build -t sergm1/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sergm1/multi-client
  - docker push sergm1/multi-nginx
  - docker push sergm1/multi-server
  - docker push sergm1/multi-worker
